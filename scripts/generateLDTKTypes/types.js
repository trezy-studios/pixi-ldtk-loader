/**
 * @typedef {import('listr2').ListrContext} ListrContext
 * @property {string | null} jsOutput JavaScript string.
 * @property {string} ldtkJSONSchema LDtk JSON Schema.
 * @property {string | null} schemaURL The URL from which to retrieve the LDtk JSON Schema.
 * @property {string | null} tsOutput Typescript string.
 */

export const types = {}
